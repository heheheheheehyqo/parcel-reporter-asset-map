{
  "name": "test",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist/*",
    "build": "parcel build src/*.html"
  },
  "devDependencies": {
    "parcel": "^2.0.0",
    "parcel-reporter-asset-map": "file:../.."
  },
  "browserslist": [
    "Chrome 40"
  ],
  "assetMap": {
    "groups": [
      "default",
      "critical"
    ]
  }
}
